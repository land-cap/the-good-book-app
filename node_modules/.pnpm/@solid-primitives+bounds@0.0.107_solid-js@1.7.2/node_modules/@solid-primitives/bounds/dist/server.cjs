'use strict';

require('solid-js');
require('@solid-primitives/utils');
require('@solid-primitives/resize-observer');
require('@solid-primitives/event-listener');

// src/index.ts
var NULLED_BOUNDS = {
  top: null,
  left: null,
  bottom: null,
  right: null,
  width: null,
  height: null
};
function getElementBounds(element) {
  {
    return Object.assign({}, NULLED_BOUNDS);
  }
}
function createElementBounds(target, { trackMutation = true, trackResize = true, trackScroll = true } = {}) {
  {
    return Object.assign({}, NULLED_BOUNDS);
  }
}

exports.createElementBounds = createElementBounds;
exports.getElementBounds = getElementBounds;
